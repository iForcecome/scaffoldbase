// v2 Day 0 占位：v1 spec 状态显示（含 design tokens / sf-* 校验）已删除。
// β7 阶段重写时再决定要展示哪些 v2 指标。
export function SpecStatus() {
  return (
    <div className="p-3 border-t border-surface-3 text-[11px] text-ink-3">
      <p>Spec 状态</p>
      <p className="text-ink-4 mt-1">β7 重写中</p>
    </div>
  )
}
