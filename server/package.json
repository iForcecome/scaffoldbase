{
  "name": "server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=.env src/index.ts",
    "build": "tsc -b",
    "start": "node dist/index.js",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "migrate:schema": "tsx --env-file=.env src/scripts/migrate-pages-to-schema.ts"
  },
  "dependencies": {
    "fastify": "^5",
    "@fastify/cors": "^11",
    "@fastify/multipart": "^9",
    "@fastify/swagger": "^9",
    "@fastify/swagger-ui": "^5",
    "drizzle-orm": "^0.44",
    "postgres": "^3",
    "zod": "^3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31",
    "tsx": "^4",
    "typescript": "~5.9.3",
    "@types/node": "^24",
    "playwright": "^1.49"
  }
}
